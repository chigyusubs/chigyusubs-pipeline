[project]
name = "chigyusubs"
version = "0.1.0"
description = "Japanese variety show subtitle pipeline"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends._legacy:_Backend"
